# 🎨 Admin Console - Visual Guide

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        ENHANCED CHAT SYSTEM                          │
└─────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│                            SERVER (Port 5000)                          │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                        Server.java                               │ │
│  │  • ServerSocket listening on port 5000                          │ │
│  │  • Manages all connections                                       │ │
│  │  • Stores chat history (last 100 messages)                      │ │
│  │  • Tracks statistics                                            │ │
│  │  • Thread-safe collections (ConcurrentHashMap)                  │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│  │ClientHandler │  │ClientHandler │  │ClientHandler │               │
│  │   (Admin)    │  │  (Client 1)  │  │  (Client 2)  │               │
│  │ isAdmin=true │  │ isAdmin=false│  │ isAdmin=false│               │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘               │
└─────────┼──────────────────┼──────────────────┼───────────────────────┘
          │                  │                  │
          │ TCP Socket       │ TCP Socket       │ TCP Socket
          │ (Serialized      │ (Serialized      │ (Serialized
          │  Objects)        │  Objects)        │  Objects)
          │                  │                  │
┌─────────▼──────────┐ ┌─────▼────────┐ ┌─────▼────────┐
│                    │ │              │ │              │
│   ADMIN CONSOLE    │ │ Chat Client  │ │ Chat Client  │
│     (GUI App)      │ │  (Console)   │ │  (Console)   │
│                    │ │              │ │              │
│ ┌────────────────┐ │ │  Username:   │ │  Username:   │
│ │ Connected      │ │ │  Alice       │ │  Bob         │
│ │ Clients Tab    │ │ │              │ │              │
│ │ • Alice        │ │ │ > Hello!     │ │ > Hi Alice!  │
│ │ • Bob          │ │ │              │ │              │
│ └────────────────┘ │ │              │ │              │
│ ┌────────────────┐ │ └──────────────┘ └──────────────┘
│ │ Chat History   │ │
│ │ Tab            │ │
│ │ [10:15] Alice: │ │
│ │   Hello!       │ │
│ └────────────────┘ │
│ ┌────────────────┐ │
│ │ Statistics Tab │ │
│ │ Users: 2       │ │
│ │ Messages: 57   │ │
│ └────────────────┘ │
│ ┌────────────────┐ │
│ │ Admin Actions  │ │
│ │ Kick User: ___ │ │
│ │   [Kick]       │ │
│ └────────────────┘ │
└────────────────────┘
```

## Message Flow Diagrams

### 1. Admin Login Flow

```
Admin Console                Server                      
     │                         │                         
     │  1. Connect Socket      │                         
     ├────────────────────────>│                         
     │                         │                         
     │  2. ADMIN_LOGIN         │                         
     │     "admin:admin123"    │                         
     ├────────────────────────>│                         
     │                         │ 3. Verify credentials   
     │                         │    ✓ Match Constants    
     │                         │                         
     │  4. ADMIN_AUTH_SUCCESS  │                         
     │<────────────────────────┤                         
     │                         │                         
     │  5. CONNECTED_CLIENTS   │                         
     │     LIST                │                         
     │<────────────────────────┤                         
     │                         │                         
     │  6. CHAT_HISTORY_DATA   │                         
     │<────────────────────────┤                         
     │                         │                         
     │  7. SYSTEM_STATS_DATA   │                         
     │<────────────────────────┤                         
     │                         │                         
     │  ✓ Now Connected!       │                         
     │  ✓ All Tabs Populated   │                         
     │  ✓ Listening for updates│                         
```

### 2. Client Join Event (Real-time Update)

```
New Client           Server              Admin Console
     │                 │                      │
     │  1. Connect     │                      │
     ├────────────────>│                      │
     │                 │                      │
     │  2. LOGIN       │                      │
     │    "Alice"      │                      │
     ├────────────────>│                      │
     │                 │ 3. Add to clients    │
     │                 │    map               │
     │                 │                      │
     │  4. Success     │ 5. Broadcast         │
     │<────────────────┤    USER_JOINED       │
     │                 ├─────────────────────>│ (to all clients)
     │                 │                      │
     │                 │ 6. updateAdminCon-   │
     │                 │    soles()           │
     │                 │                      │
     │                 │ 7. CONNECTED_CLIENTS │
     │                 │    LIST (updated)    │
     │                 ├─────────────────────>│
     │                 │                      │
     │                 │                      │ 8. Display update
     │                 │                      │    ✓ New user in list!
```

### 3. Kick User Flow

```
Admin Console         Server              Target Client
     │                  │                      │
     │ 1. Enter         │                      │
     │    username      │                      │
     │                  │                      │
     │ 2. Click "Kick"  │                      │
     │                  │                      │
     │ 3. Confirm       │                      │
     │    dialog        │                      │
     │                  │                      │
     │ 4. KICK_USER     │                      │
     │    "Alice"       │                      │
     ├─────────────────>│                      │
     │                  │ 5. Find ClientHandler│
     │                  │                      │
     │                  │ 6. disconnect()      │
     │                  ├─────────────────────>│
     │                  │                      │ 7. Socket closed
     │                  │                      │    Connection lost
     │                  │                      │
     │  8. KICK_SUCCESS │ 9. Remove from       │
     │<─────────────────┤    clients map       │
     │                  │                      │
     │ 10. Confirmation │ 11. Broadcast        │
     │     dialog       │     USER_LEFT        │
     │     "Success!"   │                      │
     │                  │ 12. updateAdminCon-  │
     │                  │     soles()          │
     │                  │                      │
     │ 13. Updated      │                      │
     │     client list  │                      │
     │<─────────────────┤                      │
     │                  │                      │
     │ ✓ User removed!  │                      │
```

## Admin Console GUI Layout

```
┌────────────────────────────────────────────────────────────────┐
│  🛡️ Admin Console - Enhanced Chat System    [Connect to Server]│
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐│
│  │ [📋 Connected Clients] [💬 Chat History] [📊 Statistics] │││
│  │                      [⚙️ Admin Actions]                   │││
│  ├───────────────────────────────────────────────────────────┤│
│  │                                                           ││
│  │  [🔄 Refresh]  💡 Auto-updates when clients join/leave   ││
│  │                                                           ││
│  │  ┌─── Connected Clients ─────────────────────────────┐   ││
│  │  │                                                    │   ││
│  │  │  === Connected Clients ===                        │   ││
│  │  │  1. Alice (192.168.1.5)                          │   ││
│  │  │  2. Bob (192.168.1.6)                            │   ││
│  │  │  3. Charlie (192.168.1.8)                        │   ││
│  │  │  =========================                        │   ││
│  │  │                                                    │   ││
│  │  │                                                    │   ││
│  │  │                                                    │   ││
│  │  │                                                    │   ││
│  │  └────────────────────────────────────────────────────┘   ││
│  │                                                           ││
│  └───────────────────────────────────────────────────────────┘│
│                                                                 │
├────────────────────────────────────────────────────────────────┤
│  🟢 Connected to localhost:5000                                │
└────────────────────────────────────────────────────────────────┘

                        CHAT HISTORY TAB
┌────────────────────────────────────────────────────────────────┐
│  [🔄 Refresh]  [🗑️ Clear Display]                              │
│                                                                 │
│  ┌─── Chat History (Last 100 messages) ──────────────────────┐│
│  │                                                            ││
│  │  === Chat History ===                                     ││
│  │  [2025-11-11 10:15:23] [Alice → All]: Hello everyone!    ││
│  │  [2025-11-11 10:15:45] [Bob → Alice]: Hi there!          ││
│  │  [2025-11-11 10:16:12] [Charlie → All]: Good morning!    ││
│  │  [2025-11-11 10:16:30] [Alice → Bob]: How are you?       ││
│  │  [2025-11-11 10:17:05] [Bob → All]: Great, thanks!       ││
│  │  ====================                                      ││
│  │                                                            ││
│  │                                                            ││
│  └────────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────────┘

                       STATISTICS TAB
┌────────────────────────────────────────────────────────────────┐
│  [🔄 Refresh]  💡 Shows real-time server statistics            │
│                                                                 │
│  ┌─── System Statistics ────────────────────────────────────┐ │
│  │                                                           │ │
│  │  === System Statistics ===                               │ │
│  │  Server Start Time: 2025-11-11T10:00:00                 │ │
│  │  Active Users: 3                                         │ │
│  │  Total Messages Sent: 57                                 │ │
│  │  Files Transferred: 5                                    │ │
│  │  Chat History Size: 57                                   │ │
│  │  Admin Consoles Connected: 1                             │ │
│  │  ========================                                │ │
│  │                                                           │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘

                      ADMIN ACTIONS TAB
┌────────────────────────────────────────────────────────────────┐
│                                                                 │
│  ┌─── ⚠️ Kick User ──────────────────────────────────────────┐│
│  │                                                            ││
│  │  Username: [_Alice____________]  [❌ Kick User]           ││
│  │                                                            ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌─── ℹ️ Instructions ──────────────────────────────────────┐ │
│  │                                                           │ │
│  │  • To kick a user, enter their exact username           │ │
│  │  • Use the 'Connected Clients' tab to see all users     │ │
│  │  • Chat history is limited to 100 most recent messages  │ │
│  │  • All tabs auto-refresh when significant events occur  │ │
│  │  • System statistics show real-time server metrics      │ │
│  │                                                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

## Thread Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         SERVER                               │
│                                                              │
│  Main Thread                                                 │
│  ├─ ServerSocket.accept() ← Waiting for connections         │
│  │                                                           │
│  └─> New connection arrives                                 │
│       │                                                      │
│       ├─> Create ClientHandler                              │
│       └─> Start new Thread                                  │
│                                                              │
│  ClientHandler Thread 1 (Admin)                             │
│  ├─ Receive ADMIN_LOGIN                                     │
│  ├─ Authenticate                                            │
│  ├─ Send initial data                                       │
│  └─ Loop: Handle admin commands                             │
│                                                              │
│  ClientHandler Thread 2 (Client "Alice")                    │
│  ├─ Receive LOGIN                                           │
│  ├─ Add to clients map                                      │
│  ├─ Notify all clients                                      │
│  ├─ Update admin consoles ← Server.updateAdminConsoles()    │
│  └─ Loop: Handle messages                                   │
│                                                              │
│  ClientHandler Thread 3 (Client "Bob")                      │
│  └─ (same as above)                                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    ADMIN CONSOLE                             │
│                                                              │
│  Main Thread (EDT - Event Dispatch Thread)                  │
│  ├─ UI Rendering                                            │
│  ├─ Handle button clicks                                    │
│  ├─ Update UI components                                    │
│  └─ Must use SwingUtilities.invokeLater()                   │
│                                                              │
│  Connection Thread                                          │
│  ├─ Created when "Connect" clicked                          │
│  ├─ Creates Socket                                          │
│  ├─ Sends ADMIN_LOGIN                                       │
│  ├─ Waits for response                                      │
│  └─ Updates UI via SwingUtilities.invokeLater()             │
│                                                              │
│  Listener Thread                                            │
│  ├─ While connected:                                        │
│  │   ├─ Read Message from input stream                     │
│  │   └─ Call handleServerMessage()                         │
│  │       └─> SwingUtilities.invokeLater() {                │
│  │           ├─ Update clientListArea                      │
│  │           ├─ Update chatHistoryArea                     │
│  │           └─ Update systemStatsArea                     │
│  │           }                                              │
│  └─ Loop continues until disconnect                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Data Structures

```
SERVER DATA STRUCTURES
═════════════════════════════════════════════════════════════

┌─ ConcurrentHashMap<String, ClientHandler> clients ─────────┐
│  Key: Username        Value: ClientHandler                  │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ "Alice"    →  ClientHandler { socket, streams, ... }  │ │
│  │ "Bob"      →  ClientHandler { socket, streams, ... }  │ │
│  │ "Charlie"  →  ClientHandler { socket, streams, ... }  │ │
│  └───────────────────────────────────────────────────────┘ │
│  Thread-safe for concurrent access                          │
└─────────────────────────────────────────────────────────────┘

┌─ CopyOnWriteArrayList<Message> chatHistory ────────────────┐
│  ┌───────────────────────────────────────────────────────┐ │
│  │ Message { type, sender, receiver, content, timestamp }│ │
│  │ Message { ... }                                        │ │
│  │ Message { ... }                                        │ │
│  │ ... (up to 100 messages)                              │ │
│  └───────────────────────────────────────────────────────┘ │
│  Thread-safe for concurrent reads/writes                    │
└─────────────────────────────────────────────────────────────┘

┌─ ConcurrentHashMap.newKeySet<Socket> adminSockets ─────────┐
│  ┌───────────────────────────────────────────────────────┐ │
│  │ Socket { address: 127.0.0.1, port: 52341 }           │ │
│  │ Socket { address: 127.0.0.1, port: 52342 }           │ │
│  │ Socket { address: 127.0.0.1, port: 52343 }           │ │
│  └───────────────────────────────────────────────────────┘ │
│  Set of all connected admin console sockets                 │
└─────────────────────────────────────────────────────────────┘

┌─ Statistics (primitive types) ──────────────────────────────┐
│  int totalMessagesSent = 57;                                 │
│  int totalFilesTransferred = 5;                              │
│  LocalDateTime serverStartTime = ...;                        │
└─────────────────────────────────────────────────────────────┘
```

## Class Relationships

```
┌──────────────────┐
│    Runnable      │  (Interface)
└────────┬─────────┘
         │ implements
         │
┌────────▼─────────┐
│  ClientHandler   │
├──────────────────┤
│ - socket         │
│ - username       │
│ - input/output   │
│ - isAdmin        │
├──────────────────┤
│ + run()          │
│ + sendMessage()  │
│ + disconnect()   │
└──────────────────┘

┌──────────────────┐
│  Serializable    │  (Interface)
└────────┬─────────┘
         │ implements
         │
┌────────▼─────────┐
│    Message       │
├──────────────────┤
│ - type           │
│ - sender         │
│ - receiver       │
│ - content        │
│ - timestamp      │
├──────────────────┤
│ + toString()     │
│ + getFormatted...│
└──────────────────┘

┌──────────────────┐
│     JFrame       │
└────────┬─────────┘
         │ extends
         │
┌────────▼─────────┐
│  AdminConsole    │
├──────────────────┤
│ - socket         │
│ - connected      │
│ - GUI components │
├──────────────────┤
│ + connectToSrv() │
│ + kickUser()     │
│ + requestData()  │
└──────────────────┘

┌──────────────────┐
│     Server       │
├──────────────────┤
│ - serverSocket   │  ← Has many ─┐
│ - clients        │              │
│ - adminSockets   │  ← Has many ─┤
│ - chatHistory    │              │
│ - statistics     │              │
├──────────────────┤              │
│ + addClient()    │              │
│ + kickUser()     │              │
│ + broadcast()    │              │
│ + updateAdmins() │              │
└──────────────────┘              │
         │                         │
         │ Creates                 │
         │                         │
┌────────▼─────────┐               │
│  ClientHandler   │ ◄─────────────┘
└──────────────────┘
```

## File Organization

```
EnhancedChatApp/
│
├── src/
│   ├── common/                     [Shared Classes]
│   │   ├── Constants.java          • Server config
│   │   ├── Message.java            • Universal message format
│   │   └── MessageType.java        • Message type constants
│   │
│   ├── server/                     [Server-side]
│   │   ├── Server.java             • Main server logic
│   │   ├── ClientHandler.java      • Per-client thread
│   │   ├── AdminConsole.java       • Admin GUI ⭐
│   │   └── PrivateChatHandler.java (not used)
│   │
│   └── client/                     [Client-side]
│       ├── Client.java             • Test client
│       ├── ClientListener.java     • Message receiver
│       └── ui/                     (empty - for future GUI client)
│
├── docs/                           [Documentation]
│   ├── QUICKSTART.md               • Getting started guide
│   ├── ADMIN_ARCHITECTURE.md       • Technical deep-dive
│   ├── IMPLEMENTATION_SUMMARY.md   • What was built
│   └── VISUAL_GUIDE.md             • This file!
│
├── bin/                            [Compiled .class files]
│   └── (created by build.bat)
│
├── build.bat                       [Build script]
├── run-server.bat                  [Start server]
├── run-admin.bat                   [Start admin console]
├── run-client.bat                  [Start test client]
│
└── README.md                       [Main documentation]
```

---

**This visual guide provides diagrams and layouts to help understand the Admin Console architecture!** 📊
